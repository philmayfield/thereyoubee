import React from "react";
import RegLogin from "../regLogin/RegLogin";

const Register = () => {
  return (
    <div className="register-view">
      <RegLogin isRegister={true} />
    </div>
  );
};

export default Register;
